version: '3.8'

services:
  app:
    build: .
    volumes:
      - ./data:/app/data
      - ./config:/app/config
    environment:
      - BIOTICA_ENV=development
    command: biotica

  test:
    build: .
    volumes:
      - ./tests:/app/tests
      - ./coverage:/app/coverage
    command: pytest
