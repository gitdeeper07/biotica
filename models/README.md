# BIOTICA Models

This directory contains trained model artifacts.

## Structure
- `mi_cnn_v1/` - Primary MI-CNN classifier
- `biome_thresholds/` - Per-biome normalization parameters
- `bayesian_weights/` - Stan posterior samples for weights
