"""Setup script for BIOTICA."""

from setuptools import setup, find_packages

setup(
    name="biotica",
    version="1.0.0",
    packages=find_packages(where="src"),
    package_dir={"": "src"},
    install_requires=[
        "numpy>=1.21.0",
        "scipy>=1.7.0",
    ],
    extras_require={
        "dev": [
            "pytest>=6.2.0",
            "pytest-cov>=2.12.0",
            "black>=21.0.0",
        ],
    },
    python_requires=">=3.8",
    author="Samir Baladi",
    author_email="gitdeeper@gmail.com",
    description="BIOTICA - Biotechnology Framework",
    license="MIT",
)
